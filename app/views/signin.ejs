<!DOCTYPE html>
<html>
    <head>
        <title>
            <%=title %>
        </title>
        <link rel="stylesheet" href="signInStyle.css" media="screen">
    </head>
    <body>
        <% for (var i in messages) { %>
            <div class="flash"><%= messages[i] %></div>
        <% } %>
        <h1>Welcome home, young padawan!</h1>
        <form id="SignInForm" class="formClass" action="/signin" method="post">
            <div>
                <label>Username:</label>
                <input type="text" name="username" />
            </div>
            <div>
                <label>Password:</label>
                <input type="password" name="password" \>
            </div>
            <div>
                <input id="buttonSubmit" onclick="alert('Successfully signed in!')" type="submit" value="Sign In" />
            </div>
        </form>
    </body>
</html>
